/*
nextflow config for drugmr pipeline!!
*/

// work -> temp storage for stuff outside nf workflow thus...
workDir = './tmp/nf-work'

params {
    image_name = 'ghcr.io/guillermocomesanacimadevila/drugmr:latest'
    out_dir = 'results'
    inputs = null   
    qtl_manifest = 'assets/qtl_manifest.csv'
    runs_root = 'runs'
    trace_timestamp = new java.util.Date().format('yyyy-MM-dd_HH-mm-ss')
    run_id = "${params.inputs[0].pheno_id}_${params.inputs[0].pqtl_dataset}_${params.trace_timestamp}_${'git rev-parse --short=7 HEAD'.execute().text.trim()}"
    tracedir = "${params.runs_root}/${params.run_id}/pipeline_info"
}

// temp 
docker {
    enabled = true
}


/*
--------------------------
Profiles for containerisation
--------------------------
*/ 

profiles {

    /*
    falcon
    */

    docker {
        docker.enabled = true
        singularity.enabled = false
        podman.enabled = false
        shifter.enabled = false
        charliecloud.enabled = false
    }

    singularity {
        singularity.enabled = true
        singularity.autoMounts = true
        docker.enabled = false
        podman.enabled = false
        shifter.enabled = false
        charliecloud.enabled = false
    }

    apptainer {
        apptainer.enabled = true
        apptainer.autoMounts = true
        docker.enabled = false
        podman.enabled = false
        shifter.enabled = false
        charliecloud.enabled = false
    }

    podman {
        podman.enabled         = true
        docker.enabled         = false
        singularity.enabled    = false
        shifter.enabled        = false
        charliecloud.enabled   = false
    }

    shifter {
        shifter.enabled        = true
        docker.enabled         = false
        singularity.enabled    = false
        podman.enabled         = false
        charliecloud.enabled   = false
    }

    charliecloud {
        charliecloud.enabled   = true
        docker.enabled         = false
        singularity.enabled    = false
        podman.enabled         = false
        shifter.enabled        = false
    }
}

timeline {
    enabled = true
    file    = "${params.tracedir}/execution_timeline_${params.trace_timestamp}.html"
}

report {
    enabled = true
    file    = "${params.tracedir}/execution_report_${params.trace_timestamp}.html"
}

trace {
    enabled = true
    file    = "${params.tracedir}/execution_trace_${params.trace_timestamp}.txt"
}

dag {
    enabled = true
    file    = "${params.tracedir}/pipeline_dag_${params.trace_timestamp}.svg"
}

manifest {
    name            = 'drugmr'
    author          = 'Guillermo Comesaña Cimadevila (Marie-Joe Dib, Christian Pepler, Dervis Salih, Nicholas Bray, Emily Simmonds, Valentina Escott-Price)'
    homePage        = 'https://github.com/guillermocomesanacimadevila/drugMR'
    description     = 'drugMR -> for now'
    mainScript      = 'main.nf'
    nextflowVersion = '!>=21.04.0'
    version         = '1.0'
}   